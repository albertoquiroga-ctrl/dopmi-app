# Guardián — contrato de cobro de prueba

Decisión confirmada por el titular el 23 de septiembre de 2026: cobrar al activar y después cada mes, solamente si puede asignarse completo el neto a gastos aprobados; sin saldo comunitario ni deuda por meses omitidos.

## Experiencia y estados

- Antes de confirmar: mostrar importe bruto mensual en MXN ($50/$200/$500 o personalizado), primera fecha de cobro (hoy), fecha aproximada siguiente, comisión y costo de Stripe estimado como tal, posibilidad de cambiar monto y cancelar. Exigir confirmación explícita; no tomar como autorización la selección de modo donante.
- Sin capacidad inicial suficiente: no abrir Checkout ni crear suscripción; mostrar que Guardián aún no se pudo activar, sin cargo. Permitir reintentar.
- Con capacidad: el servidor crea una reserva idempotente y un Checkout de pago único inicial en modo de prueba. Una sesión abandonada o vencida libera la reserva; un pago inicial sin confirmar no se presenta como aportación confirmada. Tras conciliar el primer pago, crea el calendario mensual en Stripe Billing con facturas futuras retenidas en borrador; nunca crea una suscripción de cobro automático y después intenta pausarla.
- En renovaciones: cada factura debe estar asociada a un ciclo único; comprobar y reservar capacidad antes de que Stripe intente cobrarla. Si no hay capacidad, impedir que esa factura se cobre y registrar el mes como omitido, sin deuda ni saldo a favor. Nunca usar la respuesta HTTP del webhook como única garantía de que Stripe omitirá un cobro: Stripe puede finalizar facturas aunque haya fallado la entrega de eventos.
- Si se cobró pero la capacidad dejó de estar disponible o el neto real no cabe, devolver el importe completo por el mismo medio de pago. No transferir ni contar como financiado hasta conciliar el cargo y la asignación. No asignar parcialmente un ciclo.
- Cambio de monto: informar el nuevo importe, aplicar a ciclos futuros sin prorratear el vigente. Cancelación: detener renovaciones futuras y mostrar el estado de los pagos pasados; no revertir automáticamente un pago ya entregado.

## Condiciones técnicas antes de activar

1. Una clave estable por usuario y ciclo, vinculada con `subscription_id`, `invoice_id`, `payment_intent_id`, `charge_id` y reserva, con unicidad en PostgreSQL. Sólo el servidor usa `service_role`; el cliente ve exclusivamente su historial mediante RLS.
2. Primera reserva antes de crear Checkout; reconciliar abandono, expiración y devolución. El límite superior retenido actualmente es bruto menos 2 %; al confirmar se conoce la comisión real de Stripe y se debe reducir la reserva y reasignar exactamente el neto real de forma atómica, o devolver todo.
3. Para cada renovación, usar una suscripción `send_invoice` con `pause_collection.behavior=keep_as_draft` antes de su primer ciclo. Validar que la factura está en `draft`, `auto_advance=false`, nunca intentada, con cliente, importe y suscripción correctos antes de reservar y cobrar. Probar en sandbox si puede finalizarse y pagarse una factura individual sin reactivar el cobro automático de las siguientes; si Stripe no lo permite, rediseñar antes de habilitar Guardián. No confiar sólo en `invoice.upcoming`, `invoice.created`, una pausa tardía o un webhook que responde 200. Cubrir factura inicial, reintentos, entrega duplicada o desordenada, bloqueo de concurrencia, cambio de monto, cancelación y conciliación tras pérdida de respuesta.
4. Transferir sólo el neto confirmado a las cuentas Connect correctas con claves idempotentes y evidencia persistida. Conciliar devoluciones y reversiones con los mismos criterios del hito 4.
5. Mantener `sk_test_`/`rk_test_`; no crear suscripciones en producción ni habilitar Stripe Tax automáticamente antes de resolver el tratamiento fiscal aplicable.

La reserva SQL existente **no realiza cobros**. Hasta cumplir esas condiciones, Guardián no se expone como opción activa en la app.

El validador puro `supabase/functions/_shared/guardian-billing.mjs` implementa el rechazo preventivo de facturas inseguras, verifica de nuevo la identidad y el importe después de finalizar una factura individual y genera una clave de ciclo determinista. Aún no se conecta a un endpoint de cobro. Stripe documenta que la factura inicial de una suscripción de cobro automático se finaliza de inmediato y que una renovación puede finalizarse pese a fallas prolongadas del webhook; por eso el control no puede depender de pausar una suscripción después de crearla. Véase [facturación de suscripciones](https://docs.stripe.com/billing/invoices/subscription) y [pausar cobros](https://docs.stripe.com/billing/subscriptions/pause-payment).

## Experimento aislado en Stripe de prueba (24 de septiembre de 2026)

- En el entorno de prueba de DopMi creé el reloj `clock_1UJ06k2ZjyMOQ0uLcb0LUBio`, un cliente de prueba sin medio de pago y la suscripción `sub_1UJ08t2ZjyMOQ0uLcijjnhDr` con `collection_method=send_invoice` y un día de prueba. Antes de su primer ciclo de pago, Stripe confirmó `pause_collection={behavior:keep_as_draft,resumes_at:null}`.
- Cancelé inmediatamente esa suscripción: Stripe confirmó `status=canceled`. No hubo intento de cobro a una persona ni se usó modo real.
- La conexión API disponible no expone adelantar el reloj de prueba ni pagar una factura. Tras preparar una segunda suscripción, la persona titular adelantó el reloj manualmente al 25 de septiembre de 2026, 01:05 UTC.
- La suscripción `sub_1UJ0mY2ZjyMOQ0uLww3GOJag` produjo la factura de renovación `in_1UJ0vp2ZjyMOQ0uLQmm5EaZG` por 5000 centavos MXN. Stripe confirmó `status=draft`, `auto_advance=false`, `attempted=false`, `amount_paid=0`, `billing_reason=subscription_cycle` y la suscripción `active`, con `pause_collection.behavior=keep_as_draft` y sin `resumes_at`.
- Stripe permitió finalizar **únicamente esa factura**, con `auto_advance=false`; quedó `open`, `attempted=false`. La suscripción siguió pausada. Después anulé esa factura (`status=void`, `amount_paid=0`) y cancelé la suscripción (`status=canceled`). No hubo cargo.
- **Pago individual y renovación posterior verificados por API el 24 de septiembre de 2026:** la tercera suscripción aislada `sub_1UJ1iv2ZjyMOQ0uLKEGSvbck` (metadata `guardian_individual_invoice_payment`) tiene una factura de 5000 centavos MXN `in_1UJ1pT2ZjyMOQ0uL81pzKuhJ` con `status=paid`, `amount_paid=5000`, `amount_remaining=0` y `attempt_count=1`. Su InvoicePayment está pagado y vinculado al PaymentIntent de prueba `pi_3UJ1sM2ZjyMOQ0uL0gxWjBrO`; `livemode=false`.
- Después del avance manual del reloj a octubre, la factura siguiente `in_1UJ1zH2ZjyMOQ0uLHEQ3y9og` permanece `draft`, `auto_advance=false`, `attempted=false`, `attempt_count=0`, `amount_paid=0` y `next_payment_attempt=null`. Ambas facturas pertenecen a la misma suscripción. La suscripción aislada está `canceled` y conserva `pause_collection.behavior=keep_as_draft`, sin reanudación programada. Esta verificación recupera la prueba ya realizada; no crea otro cobro.
- **Pendiente:** integrar este mecanismo con reservas y asignación del neto real, devolución, autorización del usuario, y probar falla/reintento del servidor y conciliación tras pérdida de respuesta. El experimento de Stripe por sí solo no acredita el flujo completo de Guardián ni habilita dinero real.

## Lectura de evidencia de pago para el servidor

`readGuardianPaidEvidence` recibe una instancia StripeClient configurada con API `2026-08-26.dahlia` y `lookupSubscription`, que debe consultar `dopmi_guardian_subscription_server` con operación `lookup` usando exclusivamente `service_role`. Solo el ID de factura entra a la lectura; propietario, precio e importe esperado proceden del registro privado. Obtiene la factura con `expand: [payments]` y el PaymentIntent con `expand: [latest_charge.balance_transaction]`. Listas incompletas o varios pagos exitosos requieren revisión; nunca se ignora una página para calcular el neto.

La salida acredita identidad e importes del pago consultado, no autoriza asignaciones. La liquidación descrita abajo bloquea y verifica el enlace factura/ciclo y su reserva en PostgreSQL. Cancelar una suscripción no impide consultar evidencia de un pago pasado, pero sigue impidiendo preparar otro cobro. El lector se conecta al procesador mediante `guardianService`; por sí solo no crea operaciones Stripe.

## Liquidación y trabajos de pagos confirmados

La RPC exclusiva de servidor `dopmi_guardian_settlement_server` exige una factura ya vinculada a un ciclo y verifica donante, suscripción, importes y unicidad de los identificadores. Bajo bloqueos de rescatistas en orden, convierte toda la reserva en asignaciones del neto real, o registra devolución completa. El costo Stripe lo absorbe Dopmi al devolver todo; la comisión de plataforma queda en cero. Los importes públicos y las reservas individuales incluyen las asignaciones Guardián.

`guardianService` obtiene evidencia de Stripe, liquida y procesa una cola privada de transferencias/devoluciones. Los intentos usan arrendamiento de cinco minutos, máximo ocho intentos y ventana inferior a 23 horas desde el primero. Un resultado incierto después de esa ventana requiere revisión. La devolución consulta primero las existentes y no marca éxito mientras esté pendiente. Una transferencia requiere identidad de cargo, ausencia de disputa/devolución, destino persistido y cuenta habilitada.

El trabajador se integra al cron existente solo con `DOPMI_GUARDIAN_WORKER_ENABLED=true`, clave H4 de prueba y migración aplicada. La acción autenticada `reprocess_guardian_invoice` acepta un ID de factura previamente vinculada. Ese procesador de facturas no paga facturas ni activa suscripciones. El primer Checkout tiene su propio recorrido, descrito abajo. Faltan el calendario y cobro mensual condicionado, la reversión de transferencias ante devoluciones posteriores y la aceptación remota antes de habilitarlo.


## Primer pago: alta privada y Checkout

`guardianActivationService` y `dopmi_guardian_activation_server` preparan el primer pago con consentimiento versionado `guardian-2026-09-24`. El servidor identifica al donante; nunca se toma de metadata de un evento o de un parámetro de retorno. La reserva completa precede a cualquier llamada Stripe. Sin capacidad se registra `no_capacity` y no se crea Checkout ni suscripción.

La petición de Checkout es un pago único MXN, guarda el medio de pago para uso posterior con `setup_future_usage=off_session`, conserva la misma clave de idempotencia y vence a los 35 minutos. La reserva dura 40 minutos para dar margen a la conciliación. Dos dispositivos no pueden mantener dos altas pendientes del mismo donante; una concesión de ejecución de dos minutos, ocho intentos y una ventana máxima de 23 horas acotan los reintentos. Las respuestas perdidas se recuperan con la misma petición. Un intento incierto fuera de esa ventana queda en `attention`; nunca se abre otro cobro para resolverlo.

La sesión persistida vincula el pago a su reserva. Al confirmar se vuelve a consultar Checkout, PaymentIntent, cargo y comisión; se validan identidad, modo prueba, importes, cliente y medio de pago. `settle_initial` comparte la liquidación atómica y los trabajos de transferencia/devolución de las renovaciones, sin inventar una factura o suscripción para el pago inicial. Expiración o fallo asíncrono libera la reserva; cualquier pago tardío se devuelve completo, incluso si ya existe otra solicitud de alta.

El webhook firmado atiende sesiones conocidas de Guardián y el trabajador recupera altas pendientes bajo `DOPMI_GUARDIAN_WORKER_ENABLED`. El código no expone todavía un endpoint de alta para la app. Un pago inicial asignado queda en `funded_pending_schedule` hasta que el servicio de calendario descrito abajo verifica y registra Billing; entonces devuelve `active`. Antes de exponerlo faltan implementar cobro condicionado/cambio/cancelación y completar la aceptación de Stripe. Stripe Tax sigue desactivado conforme al alcance de prueba existente.


## Calendario mensual protegido después del primer pago

`guardianScheduleService` y `dopmi_guardian_schedule_server` sólo preparan Billing cuando el primer pago quedó asignado completo, todas sus transferencias terminaron y la cuenta sigue habilitada. Se vuelven a consultar cargo, cliente y medio de pago en Stripe. El cargo inicial no puede estar disputado o reembolsado y el medio debe seguir perteneciendo al cliente guardado.

Se crea un precio mensual en MXN y una suscripción `send_invoice`, con `proration_behavior=none`, sin factura inicial y con `cancel_at_period_end=true`. El ancla conserva día/hora UTC del cargo inicial mediante `billing_cycle_anchor_config`; se contemplan meses cortos y años bisiestos. Sólo una actualización conjunta que establece `pause_collection.behavior=keep_as_draft` y retira la cancelación puede dejarla preparada para futuras facturas. Se vuelve a leer la suscripción antes de registrar la relación en PostgreSQL. Si el servidor pierde conectividad antes de esa actualización, permanece la cancelación de protección; no se abre una segunda suscripción para recuperarse. Una pausa preexistente inesperada, especialmente con `resumes_at`, no se sobrescribe.

Precio y suscripción se guardan por etapas con claves estables, concesión de ejecución de cinco minutos, ocho intentos y ventana de reintento inferior a 23 horas. La creación empieza con más de 48 horas hasta la primera renovación; fechas ya próximas o antiguas requieren revisión. Webhooks firmados y lectura periódica sincronizan cancelaciones y detectan configuraciones alteradas. Las cancelaciones no borran pagos pasados.

Este paso está protegido adicionalmente por `DOPMI_GUARDIAN_SCHEDULE_ENABLED=true`, junto con el flag del trabajador y la clave de prueba. Todavía no paga facturas, no habilita un endpoint de alta en la app y no implementa solicitudes de cambio/cancelación desde el cliente. La creación protegida se comprobó aisladamente en Stripe test como se describe abajo; falta aceptar el recorrido integrado antes de habilitarlo. Las pruebas locales usan Stripe simulado; la prueba de concurrencia usa PostgreSQL real en CI y aprobó para `0f95d8f`. Stripe Tax continúa desactivado en este alcance.

Referencias: [configurar el ciclo y evitar la factura prorrateada inicial](https://docs.stripe.com/billing/subscriptions/billing-cycle), [cancelación al final del período](https://docs.stripe.com/billing/subscriptions/cancel) y [actualizar la pausa de cobro](https://docs.stripe.com/api/subscriptions/update).


### Comprobación aislada de creación y pausa — 24 de septiembre de 2026

En el entorno de prueba de DopMi, un cliente sintético sin medio de pago (`cus_VJr55MmEsLOYzo`) y el precio mensual de $50 MXN `price_1UJDMU2ZjyMOQ0uLOkG8OTrI` permitieron crear `sub_1UJDNX2ZjyMOQ0uLrGrkDiSp` con la misma configuración de ancla, sin prorrateo y con cancelación al final del período. Stripe devolvió `active`, `livemode=false`, `latest_invoice=null` y fin de período `1792850945` (24 de octubre de 2026, 14:09:05 UTC), correspondiente al ancla solicitada.

La actualización conjunta `pause_collection.behavior=keep_as_draft` y `cancel_at_period_end=false` conservó la fecha y la ausencia de factura. Una lectura independiente confirmó `resumes_at=null`, `cancel_at=null` y la pausa. Se canceló inmediatamente con `invoice_now=false` y `prorate=false`; respuesta final `canceled`, `latest_invoice=null`. No se creó factura ni se hizo un cargo. El precio sintético permanece activo: Stripe rechazó archivarlo porque es el precio predeterminado de su producto; no tiene una suscripción activa de esta prueba.

El primer intento con otro cliente sintético sin correo fue rechazado por Stripe antes de crear una suscripción. `send_invoice` exige correo; el flujo inicial usa Checkout con `customer_creation=always`, que recoge el correo del cliente. La aceptación integrada deberá comprobar también ese dato persistido y su ausencia/modificación. La prueba válida usó `guardian-calendar-test@example.invalid`.

Límites: llamadas aisladas del conector con especificación `2026-08-26.preview`, sin PaymentMethod, sin ejecutar el servicio Guardián desplegado ni adelantar un reloj. El runtime fija `2026-08-26.dahlia`. Esto verifica el mecanismo de protección/pausa; no acredita el alta integral, la facturación futura ni un cobro mensual. La prueba anterior de factura individual pagada y siguiente factura en borrador sigue siendo evidencia separada.


## Cobro mensual condicionado — procesador de servidor

`guardianCollectionService` lee facturas actuales de Stripe; el evento firmado sólo aporta un ID que se vuelve a consultar. La suscripción persistida y su calendario verificado determinan donante, cliente, precio, monto y medio de pago. La factura debe corresponder a un ciclo mensual, MXN, una línea íntegra sin descuentos/impuestos, sin saldo previo ni pagos, con `auto_advance=false` y suscripción retenida en borrador. La fecha de la línea y la factura no pueden estar en el futuro; después de 48 horas se omite el ciclo antiguo, sin acumular cobros. Los relojes de prueba deben estar listos y proporcionan el tiempo de Stripe.

La RPC privada `dopmi_guardian_collection_server` reserva el límite superior del neto y vincula factura/ciclo en una transacción. La unicidad por suscripción y comienzo de período rechaza una segunda factura mensual. Las etapas usan una concesión de cinco minutos; preparación/finalización/anulación permiten ocho intentos dentro de 23 horas con claves estables. Inmediatamente antes de pagar se bloquea la reserva y se vuelven a validar cuenta, calendario, disponibilidad de los gastos y al menos dos minutos de vigencia. No se amplía ni se sustituye una reserva vencida.

La autorización guarda `pay_requested_at` una sola vez. El proceso que recibe esa primera autorización puede invocar `invoices.pay` con el medio guardado y `off_session=true`; ninguna recuperación vuelve a invocarlo. Si se pierde la respuesta de autorización incluso antes de llegar a Stripe, queda para revisión. Si el pago sí ocurrió, una lectura posterior obtiene evidencia y usa la liquidación existente; un pago tardío fuera de la reserva genera devolución completa. El estado de colección `paid` significa pago conciliado, no transferencia ni depósito bancario completado.

Sin capacidad, o al invalidarse la reserva antes de intentar pagar, la factura se finaliza con `auto_advance=false` y se anula. Sólo una lectura independiente de `status=void`, `amount_paid=0`, InvoicePayment cancelado e intento cancelado sin fondos confirma `skipped`; Stripe puede conservar `amount_remaining` con el importe original, como comprobó el siguiente loop; no se elimina una factura de suscripción ni se confunde un fallo de API con omisión exitosa. La cancelación detectada antes del pago sigue ese camino y conserva historial. Cambios inesperados de identidad, monto, medio o pausa detienen la operación.

El trabajador recupera facturas vinculadas pendientes y lee páginas de 20 facturas por suscripción, con cursor persistido y orden por última revisión. Atiende `invoice.created`, `invoice.finalized`, `invoice.paid`, `invoice.payment_failed` e `invoice.voided`. Una factura con pago incierto impide iniciar otro ciclo para esa suscripción. La recuperación descrita abajo distingue rechazo, autenticación adicional, procesamiento y resultado desconocido; ningún caso autoriza un segundo intento de cobro. Falta la aceptación integrada antes de habilitar el flujo.

Se requiere `DOPMI_GUARDIAN_COLLECTION_ENABLED=true` además del trabajador de prueba; ambos continúan desactivados. No hay nuevo endpoint móvil ni activación remota. Stripe Tax permanece desactivado en el alcance de pruebas. Referencias: [finalizar sin avance automático](https://docs.stripe.com/api/invoices/finalize), [pagar una factura con el medio del cliente](https://docs.stripe.com/api/invoices/pay), [anular](https://docs.stripe.com/api/invoices/void) y [restricción de eliminación de facturas de suscripción](https://docs.stripe.com/api/invoices/delete).


## Recuperación posterior al intento mensual

`guardianRecoveryService` usa la misma concesión que el cobro para impedir que otro trabajador lo cancele mientras sigue activo. Lee la factura y la lista completa de InvoicePayments, exige un solo pago predeterminado de prueba y consulta su PaymentIntent; cliente, moneda, importe y medio deben coincidir con el vínculo persistido. No obtiene autoridad de metadata ni del texto de un error. Los IDs del intento y del pago se guardan y no pueden cambiar durante la recuperación.

- `requires_payment_method`, `requires_confirmation` y `requires_action` con cero fondos recibidos/capturables permiten cerrar el ciclo mediante `invoices.voidInvoice`. No se cancela directamente el PaymentIntent predeterminado de una factura. El motivo queda como fallo de pago, intento sin confirmar o autenticación requerida. Este bloque no muestra un reto de autenticación ni completa SCA; cierra ese ciclo y conserva el motivo para la futura interfaz y gestión del medio de pago.
- `processing` o `succeeded` todavía no reflejado en una factura pagada se vuelven a consultar, con espera persistida. No se repite `pay`, no se anula mientras se observa procesamiento y no se amplía la reserva. El resultado pagado usa la conciliación existente; si llega después de vencer la reserva, devuelve el bruto completo.
- Varias operaciones asociadas, identidades cambiadas, pagos parciales, una captura pendiente o evidencia contradictoria quedan para revisión. No se interpreta un timeout como rechazo.

Antes de anular se guarda una autorización y se relee la evidencia para detectar carreras con procesamiento/pago. La clave de anulación es estable; ocho intentos y 23 horas desde la primera autorización de anulación acotan las escrituras. Una respuesta perdida se recupera por lectura. Después de agotar los reintentos, la lectura de un estado terminal sigue permitida. La reserva sólo se libera al confirmar factura anulada, InvoicePayment cancelado e intento cancelado con `amount_received=0` y `amount_capturable=0`; una liquidación concurrente impide marcarlo omitido. Los flags de Guardián siguen desactivados.

Los webhooks incluyen `invoice.payment_action_required` e `invoice_payment.paid`; siempre se consulta el objeto actual. El trabajador periódico recupera eventos faltantes. Un procesamiento confirmado se reconoce como pendiente; un error de recuperación devuelve fallo reintentable. No se devuelven ni se guardan `client_secret` en PostgreSQL.

### Prueba aislada de anulación en Stripe — 24 de septiembre de 2026

Cliente sintético sin medio de pago `cus_VJsEmBOL7HhB8P`, factura manual `in_1UJEUo2ZjyMOQ0uLSE5vRczi` de $50 MXN y `auto_advance=false`. Al finalizar: `open`, `amount_paid=0`, pago predeterminado `inpay_1UJEVA2ZjyMOQ0uLFRSukXYG` con `amount_paid=null`, intento `pi_3UJEV92ZjyMOQ0uL1NcIStvG` en `requires_payment_method`, sin cargo ni fondos recibidos/capturables. Al anular y releer los tres objetos: factura `void`, InvoicePayment `canceled`, intento `canceled`, `amount_received=0`, `amount_capturable=0` y `latest_charge=null`. Todos en modo prueba.

**Corrección derivada de la prueba:** Stripe mantuvo `amount_due=5000`, `amount_remaining=5000`, `total=5000` y `amount_requested=5000`. La anulación hace que la factura no sea cobrable; esos campos no equivalen a deuda exigible ni tienen que volverse cero. Las pruebas anteriores que simulaban ese cero se corrigieron para reflejar la respuesta real. La aceptación de omisión comprueba los estados cancelados y que no se recibió dinero.

Límites: factura manual aislada, sin llamada a `pay`, sin rechazo bancario real ni reto 3DS, y sin ejecutar el runtime Guardián desplegado. El conector declara especificación `2026-08-26.preview`; el runtime sigue fijado en `2026-08-26.dahlia`. Esta prueba valida el mecanismo de anulación; los casos de carrera, autenticación y éxito tardío están cubiertos localmente con Stripe simulado, pendientes de aceptación integrada. Stripe Tax continúa desactivado.

Referencias: [pago predeterminado de una factura](https://docs.stripe.com/api/invoice-payment/object), [lista de InvoicePayments](https://docs.stripe.com/api/invoice-payment/list), [anulación y transiciones de factura](https://docs.stripe.com/invoicing/integration/workflow-transitions) y [webhooks de suscripciones](https://docs.stripe.com/billing/subscriptions/webhooks).


## Solicitudes del titular y control de nuevos cobros

La migración `20260924160633_guardian_owner_requests.sql` añade `dopmi_guardian_plan()` y `dopmi_guardian_request(request_kind, request_key, expected_revision, new_gross_cents, consent_version)`, ambas sólo para `authenticated`. El propietario procede exclusivamente de `auth.uid()`; no se acepta un donante ni un ID Stripe proporcionado por el cliente. La consulta devuelve el importe registrado, la revisión, la solicitud pendiente, hasta diez solicitudes recientes y si existe un pago iniciado aún por conciliar. No expone IDs Stripe, concesiones ni evidencia privada. Un administrador no obtiene acceso a planes ajenos mediante estas funciones.

- `amount` exige cuenta activa y confirmada, importe distinto entre 1,000 y 1,000,000 centavos MXN y consentimiento `guardian-2026-09-24`. Registra la intención; no sustituye el precio vigente ni cambia un ciclo ya preparado. Mientras esté pendiente, impide preparar ciclos nuevos, incluso con una lectura anterior del registro.
- `cancel` registra `cancellation_requested_at` y bloquea nuevas autorizaciones de pago antes de depender de Stripe. La cancelación sigue disponible para el titular con sesión aunque su cuenta esté suspendida o su correo ya no figure confirmado. Si había una intención de monto aún sin procesar, queda `superseded` en el historial.
- La clave de solicitud es única por titular y exige exactamente la misma carga al reintentar. La revisión rechaza confirmaciones hechas desde una vista antigua; sólo una solicitud pendiente puede existir por suscripción. El cliente no puede marcarla `applied`, modificar el historial ni cambiar directamente los registros privados.
- La cancelación y la autorización de pago bloquean la misma fila de suscripción. Si la cancelación se confirma primero, el cobro aún no autorizado se omite y se libera su reserva. Si el marcador de autorización gana primero, se conserva el pago en curso para conciliación; la respuesta muestra `payment_in_flight=true`. Nunca se borra ese marcador ni se revierte automáticamente una aportación ya entregada.
- Un cambio de monto pendiente detiene **la preparación de nuevos ciclos**; un ciclo preparado antes conserva su importe y puede completar su autorización, salvo que exista cancelación. La cola persiste la intención hasta verificar su aplicación; no se presume que Stripe haya aceptado un cambio sólo por recibirla.

Verificación local: 209/209 pruebas (21 nuevas), incluidos aislamiento entre titulares/administradores, sesiones, consentimiento, duplicados, revisiones antiguas, cuenta suspendida, anulación antes del pago y conciliación de un pago ya autorizado. Se añadieron ocho comprobaciones pgTAP de permisos y carreras en PostgreSQL para solicitudes repetidas/preparación de ciclo y cancelación contra autorización.

Este bloque completa la recepción y el control transaccional de solicitudes. Su aplicación en Stripe se implementa en el siguiente bloque. La interfaz Flutter y la aceptación integral siguen pendientes; no se habilita Guardián ni se despliegan funciones y Stripe Tax permanece desactivado.

## Aplicación de cambios y cancelaciones — 24 de septiembre de 2026

`guardian-changes.mjs` y `20260924163547_guardian_request_processing.sql` procesan intenciones persistidas mediante una concesión exclusiva por suscripción. La RPC es exclusiva de `service_role`, con `search_path` fijo. El historial privado de precios tiene RLS y el cliente sólo recibe la fecha efectiva de un cambio confirmado, sin IDs del procesador.

- **Monto:** conserva el elemento, precio previo, período, aniversario e invoice actual. Verifica cliente de prueba sin saldo, precio mensual MXN, suscripción con una sola línea, medio de pago registrado, `send_invoice`, pausa indefinida `keep_as_draft` y ausencia de impuestos, descuentos o ajustes ajenos. Crea un precio con clave estable y sustituye el del elemento existente con `billing_cycle_anchor=unchanged` y `proration_behavior=none`. Una lectura independiente confirma precio, calendario y factura antes de publicar `applied`; la fecha efectiva es el inicio del siguiente período.
- **Historial:** cada vínculo de factura conserva precio, importe y período. Preparación y conciliación consultan el precio correspondiente al período, incluso tras cambios posteriores o cancelación. Varias solicitudes confirmadas antes del mismo aniversario conservan auditoría; la revisión más reciente rige ese período. El importe de una factura ya vinculada permanece inmutable.
- **Cancelación:** la barrera local del bloque anterior detiene nuevas autorizaciones inmediatamente. El trabajador cancela sólo la suscripción registrada, con `invoice_now=false` y `prorate=false`; únicamente una consulta independiente con estado `canceled` confirma la solicitud. Una cancelación sustituye un cambio pendiente, incluso si ya había una escritura en vuelo; su resultado tardío no modifica registro ni historial. No revierte automáticamente pagos anteriores.
- **Recuperación:** las claves por solicitud y los checkpoints permiten recuperar respuestas perdidas de precio, actualización, cancelación o base de datos. Primero consulta Stripe. Las escrituras se limitan a ocho intentos y 23 horas; la confirmación por lectura sigue disponible después. Las concesiones vencidas no pueden sobrescribir a su sucesor. Una solicitud a menos de 120 segundos del aniversario, o con calendario inesperado, queda pendiente para revisión, sin reasignar silenciosamente su fecha efectiva. La gestión manual de esos casos debe quedar resuelta antes de habilitar el flujo.
- **Concurrencia:** monitoreo y cambios bloquean primero la suscripción. El monitor aplaza comprobaciones durante solicitudes pendientes y no marca como inválido un precio nuevo a partir de una lectura anterior. El trabajador periódico recupera eventos perdidos; webhooks consultan estado fresco. `DOPMI_GUARDIAN_CHANGES_ENABLED` protege esta ruta y permanece desactivado.

Verificación local: **244/244 pruebas backend**, incluidas 35 nuevas, y `deno check` de trabajador/webhook aprobados. Casos nuevos: monto/fechas, facturas antiguas, pagos ya autorizados tras cancelación, duplicados, límite de reintentos, respuestas perdidas, configuración ajena, concesiones vencidas, cancelación durante creación/actualización y monitor desactualizado. Se añaden cuatro comprobaciones pgTAP de privilegios/RLS y carreras con conexiones PostgreSQL distintas; su ejecución se registra en `progress.md`.

Prueba aislada real en **Stripe sandbox**, sin medio de pago: cliente sintético `cus_VJthu5Sjwd05bp`, suscripción `sub_1UJFvU2ZjyMOQ0uLFXQGNjEY`, precio de 5,000 a 20,000 centavos MXN. Lectura independiente confirmó mismo aniversario, mismo inicio/fin de período, pausa permanente, impuestos desactivados y `latest_invoice=null`. Después de cancelar sin factura final ni prorrateo, otra lectura confirmó `canceled`, modo de prueba y ausencia de factura. Stripe exigió correo para `send_invoice`; se usó `guardian-lifecycle@example.test`. No se creó ningún cargo. Este experimento confirma las operaciones de Stripe, no el recorrido integrado Flutter–Supabase–Stripe. Pendientes: interfaz y autenticación/medio de pago, devoluciones posteriores a transferencias y aceptación integral antes de cerrar H5.

Cierre de verificación: publicado en `27de0b3`; [CI 36032672751](https://github.com/albertoquiroga-ctrl/dopmi-app/actions/runs/36032672751) aprobó los cuatro jobs, 244 pruebas backend, 163 comprobaciones pgTAP y las carreras PostgreSQL descritas. Migración remota `20260924171515_guardian_request_processing` aplicada y comprobada: RLS/ACL, funciones con `search_path` fijo, trigger, snapshots, protección del monitor y cola vacía. El único advisory añadido fue [RLS habilitado sin políticas](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy), nivel INFO, para el historial privado que intencionalmente carece de acceso directo de clientes. No hubo nuevos WARN/ERROR; no se desplegaron funciones ni se habilitaron flags.

## Integración móvil protegida — 24 de septiembre de 2026

La ruta `/guardian` y los accesos desde perfil/historial usan `ENABLE_GUARDIAN_TEST`, desactivado por defecto. Ofrecen importes predefinidos o personalizados, primer intento hoy, siguiente aniversario estimado con ajuste de fin de mes, comisión del 2% más costos Stripe, condición de capacidad y consentimiento explícito para el primer pago, recurrencia y almacenamiento del medio en Stripe. Cambiar el importe desmarca el consentimiento. La comprobación de capacidad es informativa; la reserva del servidor sigue siendo la autorización de cobro.

`dopmi_guardian_state()` sólo devuelve el plan y el último intento del titular identificado por `auth.uid()`. El intento incluye su clave de idempotencia, monto, consentimiento y estado para continuar en otro dispositivo; excluye IDs Stripe, URL, medio de pago, concesiones y datos de otros propietarios. La lectura sigue disponible para una cuenta suspendida o sin confirmación de correo, a fin de consultar/cancelar un plan existente. El servidor conserva las restricciones de alta/cambio.

`guardian-client` valida el token con Auth, exige correo confirmado y sólo acepta `checkout` con importe entero, clave UUID y consentimiento vigente. Descarta donante, redirección y estados aportados por el cliente. Requiere simultáneamente `DOPMI_GUARDIAN_CHECKOUT_ENABLED`, `DOPMI_GUARDIAN_WORKER_ENABLED`, `DOPMI_GUARDIAN_SCHEDULE_ENABLED`, `DOPMI_GUARDIAN_COLLECTION_ENABLED` y `DOPMI_GUARDIAN_CHANGES_ENABLED`; todos permanecen desactivados. El runtime exige clave Stripe de prueba. No se despliega ni habilita este endpoint antes de la aceptación integrada.

Flutter guarda cada solicitud como una unidad JSON por cuenta antes de enviarla, conservando clave, importe, revisión y consentimiento; una respuesta perdida permite reintentar la misma operación. Un conflicto de revisión confirmado por PostgreSQL requiere consultar y autorizar de nuevo. Las pantallas no interpretan el regreso de Stripe como pago confirmado ni publican cambios de monto/cancelación optimistas. La cancelación pide confirmación y explica el pago ya iniciado y la conservación del historial. El retorno incluye acceso al plan y la app vuelve a consultar al reanudarse.

Este bloque no implementa la sustitución del medio de pago, autenticación adicional del pago, cancelación del alta antes de que exista la suscripción registrada, historial completo de ciclos/asignaciones, reactivación de planes cancelados ni gestión operativa de cambios cercanos al aniversario. La etapa de primer pago confirmado con calendario aún en preparación se muestra como tal; falta resolver su cancelación antes de habilitar el alta. Estos flujos, devoluciones posteriores a transferencias y la aceptación integrada siguen pendientes. Las pruebas de interfaz usan repositorios falsos y no acreditan un recorrido Stripe–dispositivo real.

Cierre del bloque móvil: código publicado en `4497bb2`, con correcciones `cc4ec6e` y `36c05ee`. Los cuatro jobs de [CI 36039251239](https://github.com/albertoquiroga-ctrl/dopmi-app/actions/runs/36039251239) aprobaron: 31 pruebas Flutter, 251 backend, 165 pgTAP, concurrencia PostgreSQL, backend integrado y compilaciones Android/iOS. Las correcciones separaron la espera de confirmación del estado de envío y resolvieron el análisis estático. Migración remota `20260924180505_guardian_mobile_state` verificada: sólo `authenticated`, sin parámetros de propietario, filtro `auth.uid()`, `search_path` fijo y cero altas/suscripciones. El único advisory nuevo fue [función SECURITY DEFINER accesible a authenticated](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable), esperado para esta proyección controlada del titular; no se concedió lectura directa a las tablas privadas ni acceso a planes ajenos. No se desplegaron funciones ni se habilitó ningún flag.


## Cancelación durante el alta (24 de septiembre de 2026)

`dopmi_guardian_cancel_activation(activation_key)` deriva el titular de `auth.uid()` y conserva la intención en la activación privada. Repetir la misma clave no crea nuevas solicitudes. No requiere cuenta activa o correo confirmado para detener cobros. La cancelación y `prepare`/`claim`/`ready` del calendario toman primero el mismo bloqueo asesor por ciclo; la liquidación y la cancelación comparten la fila de activación antes de tocar su reserva. Si el plan ya se registró, se usa la cancelación normal bajo el bloqueo de su fila.

Sin intento de Checkout, el cierre es local. Con una creación incierta se recupera la sesión usando los campos y la clave originales, dentro del presupuesto existente. Una sesión abierta se vence con `checkout.sessions.expire` y se vuelve a leer; errores de escritura no confirman el cierre. Si el pago ganó, la reserva liberada conduce a devolución completa. Las asignaciones ya liquidadas y las transferencias conservan su historial; no hay devolución automática posterior a la entrega.

Sin concesión del calendario no se crea una suscripción. Si ya había una creación en vuelo, se conserva su recuperación con claves estables; una suscripción vinculada se cancela con `invoice_now=false` y `prorate=false`, y sólo una lectura de estado `canceled` confirma el cierre. `ready` rechaza toda activación cuya cancelación haya ganado. Una cancelación que llega durante la pausa puede dejar temporalmente la suscripción pausada; el monitor la cancela y no se registra para cobro. El monitor también puede confirmar/cancelar un ID conocido tras alcanzar el límite de creación. Una creación incierta sin ID y fuera de ese presupuesto permanece en revisión, sin inventar una confirmación.

La proyección móvil conserva por separado `status` del pago y `cancellation_status` (`pending`, `stopped`, `attention`). Detener el alta no significa devolver el primer pago. La reactivación tras esta cancelación permanece bloqueada. Referencias: https://docs.stripe.com/api/checkout/sessions/expire y https://docs.stripe.com/api/subscriptions/cancel . Pruebas nuevas usan Stripe simulado y PostgreSQL; el recorrido integrado sigue pendiente.

Evidencia del bloque: implementación `2fc30ec` y corrección de fixture `7f18987`; CI 36043207208 con cuatro jobs aprobados, 261 pruebas backend, 168 pgTAP y dos carreras adicionales sobre PostgreSQL real. Migración remota `20260924184639_guardian_activation_cancel` verificada. Primer CI detectó que el fixture de concurrencia intentaba alterar un gasto financiado; se sustituyó por un gasto sintético independiente. Ninguna función fue desplegada ni se habilitó el flujo.


## Actualizar y autenticar el medio para futuros ciclos

`guardian-method.mjs` usa Checkout `mode=setup`, cliente y retorno fijados por servidor, moneda MXN y métodos dinámicos. El titular autoriza expresamente guardar y utilizar el medio con las condiciones vigentes; no crea otra suscripción ni una factura ni llama a `pay`. La página alojada de Stripe atiende la autenticación bancaria que requiera el SetupIntent. Referencia: https://docs.stripe.com/payments/checkout/subscriptions/update-payment-details . Se eligió este flujo acotado para conservar los controles de cobro condicionado y las solicitudes de monto/cancelación existentes.

`dopmi_guardian_method_server` es exclusiva de servicio; la frontera HTTP verifica la identidad y permite únicamente clave, revisión y consentimiento. PostgreSQL comprueba cuenta, plan, revisión y ausencia de cobros o cambios pendientes. La clave persiste antes de abrir Checkout. La sesión se vincula por ID guardado y el SetupIntent/medio deben pertenecer al cliente registrado, estar en modo test y confirmar uso `off_session`. IDs Stripe, URLs y secretos no salen en la proyección del titular. Nunca se confía en metadata del evento ni en parámetros de retorno para confirmar el cambio.

Una actualización pendiente bloquea nuevas reservas de cobro y cambios de monto; un cobro ya preparado impide iniciar la actualización. Los procesos de gestión comparten la concesión de cinco minutos en la fila de suscripción. La cancelación puede entrar inmediatamente y sustituir el cambio; una respuesta de Stripe tardía no puede publicar un medio nuevo ni reactivar el plan. Se conserva el medio inicial en la activación y el snapshot de cada factura; las fuentes de nuevas facturas y el monitor usan el nuevo medio sólo tras confirmación. La lectura del monitor incluye el medio esperado y vuelve a comprobarlo bajo bloqueo para descartar resultados antiguos.

La creación de Checkout y la actualización tienen presupuestos separados de ocho escrituras y 23 horas. El ID y los parámetros de cada intento no cambian al recuperar respuestas perdidas. Fuera del presupuesto se admite lectura para confirmar una actualización ya aplicada; una creación sin ID de resultado sigue en revisión. Una sesión abierta abandonada vence sin aplicar y permite nueva autorización. Si se requiere acción bancaria, no se aplica el medio mientras el SetupIntent no confirme `succeeded`; el titular puede continuar el mismo Checkout. Un cambio inesperado del aniversario no se confirma.

La autenticación de un medio para próximos ciclos no rescata un ciclo mensual omitido. Se mantiene la recuperación ya implementada: rechazo o `requires_action` conduce a anulación sin nuevo `pay`, y un resultado incierto permanece en conciliación. La app explica esa diferencia y sólo ofrece cambiar el medio cuando la conciliación termina. La cancelación del plan continúa disponible. Falta probar Checkout/SetupIntent/3DS en el recorrido integrado y dispositivo; las pruebas nuevas son simulaciones Stripe con PostgreSQL real en CI. No se despliega ni habilita Guardian; Stripe Tax permanece desactivado.
