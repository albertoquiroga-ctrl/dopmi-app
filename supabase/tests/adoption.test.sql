begin;
set local search_path=public,extensions;
create extension if not exists pgtap with schema extensions;
select no_plan();
insert into auth.users(id,email,raw_user_meta_data,email_confirmed_at)
select ('20000000-0000-4000-8000-00000000000'||n)::uuid,'adoption-'||n||'@example.test',
  '{"display_name":"Nombre privado","terms_version":"development-2026-09-13","terms_accepted":true}',now() from generate_series(1,4) n;
insert into private.admin_memberships(user_id) values('20000000-0000-4000-8000-000000000004');
insert into public.dopmi_adoptions(id,owner_id,pet_name,city,region,story,publisher_name,photos)
values('30000000-0000-4000-8000-000000000001','20000000-0000-4000-8000-000000000001','Luna','Monterrey','Nuevo León',
 'Luna busca una familia que le dedique tiempo.','Refugio aprobado',
 array['20000000-0000-4000-8000-000000000001/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg']);

set local role anon;
select set_config('request.jwt.claim.sub','',true);
select is((dopmi_catalog()->>'total')::int,0,'draft absent from anonymous catalog');
select is(dopmi_adoption_detail('30000000-0000-4000-8000-000000000001'),null::jsonb,'draft detail private');
select is(dopmi_public_profile('20000000-0000-4000-8000-000000000001'),null::jsonb,'unreviewed publisher private');
select throws_ok('select * from dopmi_adoptions','42501',null,'anonymous cannot read raw rows');
reset role;
set local role authenticated;
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000003',true);
select is((select count(*) from dopmi_adoptions),0::bigint,'other account cannot see draft');
select throws_ok($$select dopmi_transition_adoption('30000000-0000-4000-8000-000000000001',1,'submit')$$,'42501',null,'non-owner transition blocked');
select throws_ok($$select dopmi_review_adoption('30000000-0000-4000-8000-000000000001',1,'published')$$,'42501',null,'non-admin approval blocked');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000001',true);
select is((select count(*) from dopmi_adoptions),1::bigint,'owner can recover draft');
select throws_ok($$update dopmi_adoptions set status='published'$$,'42501',null,'direct client promotion blocked');
select throws_ok($$select dopmi_transition_adoption('30000000-0000-4000-8000-000000000001',1,'submit')$$,'22023',null,'missing upload blocks submission');
select is(dopmi_can_write_photo('20000000-0000-4000-8000-000000000001/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg'),true,'owner can upload to draft');
select is(dopmi_can_write_photo('20000000-0000-4000-8000-000000000003/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg'),false,'forged photo owner blocked');
reset role;
insert into storage.objects(bucket_id,name) values('dopmi-adoption-photos','20000000-0000-4000-8000-000000000001/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg');
set local role authenticated;
select is(dopmi_transition_adoption('30000000-0000-4000-8000-000000000001',1,'submit')->>'status','submitted','owner submits complete draft');
select is(dopmi_can_write_photo('20000000-0000-4000-8000-000000000001/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg'),false,'review locks photos');
select throws_ok($$select dopmi_save_adoption('{}','30000000-0000-4000-8000-000000000001',2)$$,'22023',null,'submitted cannot be edited behind reviewer');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000004',true);
select is((dopmi_admin_adoptions()->>'total')::int,1,'admin sees review queue');
select throws_ok($$select dopmi_review_adoption('30000000-0000-4000-8000-000000000001',1,'published')$$,'40001',null,'stale review blocked');
select is(dopmi_review_adoption('30000000-0000-4000-8000-000000000001',2,'changes_requested','Agrega más contexto')->>'story',
 'Luna busca una familia que le dedique tiempo.','corrections preserve authored fields');
select is(jsonb_array_length(dopmi_adoption_reviews('30000000-0000-4000-8000-000000000001')),1,'review history persists');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000001',true);
select is((select count(*) from dopmi_notifications where read_at is null),1::bigint,'review notification reaches owner');
select is(dopmi_transition_adoption('30000000-0000-4000-8000-000000000001',3,'submit')->>'status','submitted','correction may resubmit');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000004',true);
select is(dopmi_review_adoption('30000000-0000-4000-8000-000000000001',4,'published')->>'status','published','staff approval publishes');
reset role;
set local role anon;
select set_config('request.jwt.claim.sub','',true);
select is((dopmi_catalog('{"species":"dog","city":"monterrey","min_age":0}')->>'total')::int,1,'public filters find approved post');
select is((dopmi_catalog('{"species":"cat"}')->>'total')::int,0,'nonmatching filter excludes');
select is((dopmi_catalog('{}',2,1)->'items'), '[]'::jsonb,'catalog paginates');
select is(dopmi_adoption_detail('30000000-0000-4000-8000-000000000001') ? 'review_feedback',false,'public detail excludes private moderation');
select is(dopmi_public_profile('20000000-0000-4000-8000-000000000001')->>'name','Refugio aprobado','public name uses approved snapshot');
select is(dopmi_public_profile('20000000-0000-4000-8000-000000000001') ?| array['email','phone','account_status'],false,'public profile excludes identity data');
select is(dopmi_can_read_photo('20000000-0000-4000-8000-000000000001/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg'),true,'approved attachment can be read');
select is((select count(*) from storage.objects where bucket_id='dopmi-adoption-photos'),1::bigint,'anonymous sees approved photo through storage RLS');
reset role;
set local role authenticated;
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000002',true);
select lives_ok($$select dopmi_set_favorite('30000000-0000-4000-8000-000000000001',true)$$,'favorite saves');
select lives_ok($$select dopmi_set_favorite('30000000-0000-4000-8000-000000000001',true)$$,'favorite retry idempotent');
select is((dopmi_catalog('{"saved":true}')->>'total')::int,1,'saved catalog scoped to account');
select set_config('test.thread',dopmi_start_thread('30000000-0000-4000-8000-000000000001')::text,true);
select is(dopmi_start_thread('30000000-0000-4000-8000-000000000001')::text,current_setting('test.thread'),'contact retry reuses thread');
select is(dopmi_send_message(current_setting('test.thread')::uuid,'50000000-0000-4000-8000-000000000001','Hola, me interesa Luna')->>'body','Hola, me interesa Luna','participant sends');
select lives_ok($$select dopmi_send_message(current_setting('test.thread')::uuid,'50000000-0000-4000-8000-000000000001','Hola, me interesa Luna')$$,'message retry succeeds');
select is(jsonb_array_length(dopmi_thread_messages(current_setting('test.thread')::uuid)),1,'retry creates only one message');
select throws_ok($$select dopmi_send_message(current_setting('test.thread')::uuid,'50000000-0000-4000-8000-000000000001','Diferente')$$,'22023',null,'same id with different payload blocked');
select is((dopmi_list_threads()->>'total')::int,1,'participant sees thread list');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000003',true);
select is((select count(*) from dopmi_favorites),0::bigint,'favorites private');
select is((select count(*) from dopmi_messages),0::bigint,'third party cannot read messages');
select is((select count(*) from dopmi_threads),0::bigint,'third party cannot read threads');
select is((select count(*) from dopmi_notifications),0::bigint,'third party cannot read notifications');
select throws_ok($$select dopmi_thread_messages(current_setting('test.thread')::uuid)$$,'42501',null,'message RPC rejects nonparticipant');
select throws_ok($$select dopmi_send_message(current_setting('test.thread')::uuid,gen_random_uuid(),'Invadido')$$,'42501',null,'nonparticipant cannot send');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000004',true);
select is((select count(*) from dopmi_messages),0::bigint,'admin is not a private conversation participant');
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000001',true);
select is((select count(*) from dopmi_notifications where kind='message'),1::bigint,'only one message notification');
select lives_ok($$select dopmi_read_thread(current_setting('test.thread')::uuid)$$,'recipient reads thread');
select is((select count(*) from dopmi_notifications where kind='message' and read_at is null),0::bigint,'unread count clears');
select lives_ok($$select dopmi_close_thread(current_setting('test.thread')::uuid)$$,'participant can close');
select throws_ok($$select dopmi_send_message(current_setting('test.thread')::uuid,gen_random_uuid(),'Otro')$$,'22023',null,'closed thread blocks new messages');
select lives_ok($$select dopmi_save_adoption((select to_jsonb(a) from dopmi_adoptions a where id='30000000-0000-4000-8000-000000000001')||'{"pet_name":"Nombre editado","status":"published"}', '30000000-0000-4000-8000-000000000001',5)$$,'published content can be edited with withdrawal');
select is((dopmi_catalog()->>'total')::int,0,'edited publication disappears until reapproved');
select is((select status from dopmi_adoptions where id='30000000-0000-4000-8000-000000000001'),'draft','payload cannot choose published status');
reset role;
set local role anon;
select set_config('request.jwt.claim.sub','',true);
select is(dopmi_can_read_photo('20000000-0000-4000-8000-000000000001/30000000-0000-4000-8000-000000000001/40000000-0000-4000-8000-000000000001.jpg'),false,'withdrawn photo is private again');
select is((select count(*) from storage.objects where bucket_id='dopmi-adoption-photos'),0::bigint,'storage hides withdrawn attachment');
reset role;
update public.profiles set account_status='suspended' where id='20000000-0000-4000-8000-000000000001';
set local role authenticated;
select set_config('request.jwt.claim.sub','20000000-0000-4000-8000-000000000001',true);
select throws_ok($$select dopmi_save_adoption('{}')$$,'42501',null,'suspended cannot create drafts');
select is((select count(*) from dopmi_messages),0::bigint,'suspended cannot read messages');
reset role;
select * from finish();
rollback;
